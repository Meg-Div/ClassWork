#fruit = ["Apple", "Pear"]
#print(fruit[0])

#iterate through list and find zeros to end
#numbers = [1,2,5,0,7,0,9,0,2,0,2,3]

#numbers.sort()

#for number in numbers:
#if (number == 0):
#numbers.remove(number) 
#  #numbers.append(0)

#print(numbers)


#zeros = numbers.count(0)
#filtered = numbers.filter(lambda x: x == 0, numbers)
#print(filtered)
#arr = [0] * zeros

# for loop to iterate through the nodes, saving the node to a temp, then self.right = temp, 
# self.right =left, self.left = temp 

